/**
 * @format
 */

import {AppRegistry} from 'react-native';
import SchoolMapScreen from './App.js';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => SchoolMapScreen);
